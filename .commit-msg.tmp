fix(tts): 背压式分块播放 + 生成流水线，彻底消除卡顿

定位过程（Electron 内时间线剖析）：
- node-cpal 输出流缓冲有界（约 1.2 秒），写满会抛
  "Failed to write to stream: buffer full"——此前按墙钟前瞻写入，
  一旦设备启动延迟或消费波动就触发该错误中断播放
- 上一版逐句"读完再生成下一句"导致句间出现最长 2.2 秒静默

修复：
- PacedPlayback 改为背压式：小分块（1024 采样）持续写入，遇
  buffer full 等待下一拍重试，设备消费速度即为节奏；新增 5 秒
  停滞看门狗，设备长时间不消费时给出明确错误
- pump 改为生成流水线：整段文本共用一条持续播放队列，合成块在
  onProgress 到达时立即入队，句子边生成边出声，上一句播放期间
  并行合成下一句；句间写入空洞由设备缓冲内已排队的音频覆盖
- 时间线验证：6 句长文本 22.9 秒音频连续写入，唯一写入空洞
  (947ms) 落在第一句音频缓冲覆盖期内，全程无静默、无错误
